require 'spec_helper'

describe LeagueManager::ResultSet do

  context '.from_json' do
    it 'can crack some pipes' do
      # client = LeagueManager::Client.new(:endpoint => "127.0.0.1:3001", :api_key => "qwerty")
      # options = {:resource => "divisions/1/top_goalscorers", :params => {:id => 1, :action => "top_goalscorers"}}
      # result = client.get( options )
      # puts " Results ".red
      # player = result.first
      # ap player.id
      # ap player.goals
      # ap player.team.id
      # ap player.team.name
      
    end
  end
end